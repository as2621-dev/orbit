# Progress: phase-5-overlap-trending-scoop

**Phase file:** plans/phase-5-overlap-trending-scoop.md
**Started:** 2026-06-18
**Base HEAD:** 94a0fb5 (Phase 4)
**Baseline tests:** 83 passing
**Execution mode:** SEQUENTIAL (1→2→3→4) — file-overlap on trending.py/cluster.py/render.py/rerank.py + strict dependency chain per phase self-critique Risk lens (1). No worktree parallelism. No irreversible sub-phases.

## NOTE — stray prior-run artifacts found on disk (untracked, NOT authored by this run's sub-agents)
A prior interrupted Phase-5 run left untracked drafts on disk that this run inherited: `signals.py`, `web_search_keyless.py`, the `tag_external_corroboration`/`_classify_corroboration` half of `trending.py`, `tests/test_external_trending.py` (12 tests), and a pre-written `.agents/execution-reports/overlap-trending-scoop-sub-3.md`. They were NOT produced by a reviewed sub-agent in THIS run. Each owning sub-phase reviews its inherited drafts as UNTRUSTED (fix/rewrite/adopt deliberately), never silently accepts them. The progress file itself was partially overwritten by those stray writes and has been rewritten here to the actual verified state of this run.

## Sub-phase progress
- [x] 1: Cluster overlaps — short-merge, long-cross-link — COMPLETED (DoD PASS; cluster.py+fusion.py+dedupe.py+test_cluster.py; long-form via injectable is_long_form predicate, default = chapters-present proxy). Verified: full suite 90 passed.
- [x] 2: Internal-network trending — baseline-relative velocity — COMPLETED (DoD PASS; signals.py reviewed+adopted, trending.py compute_internal_trending+TrendingItem, test_trending.py). Baseline = batch median (rerank.compute_creator_engagement_baselines fallback; seen table holds only first_seen_at, no engagement snapshots); store injected via store_module param. Verified: full suite 110 passed.
- [x] 3: External trending — corroboration vs scoop tagging — COMPLETED (DoD PASS; web_search_keyless.py reviewed+adopted; relocated external + scoop functions out of the 733-line trending.py into NEW external_trending.py; trending.py now 395, external_trending.py 384, web_search_keyless.py 273 — all < 500). tag_external_corroboration now in lib.external_trending. Verified: full suite 115 passed.
- [x] 4: Anomaly/scoop detection + render the M3 sections — COMPLETED (DoD PASS; external_trending.detect_scoops + build_trending_multiplier_map, render.py 3 M3 sections via _render_overlap_block/_render_trending_rail/_render_scoops_strip + optional clusters/trending_items/scoops params, rerank.py multiplier wired keyword-only optional → M1/M2 byte-for-byte unchanged, html_render.py CSS-only, test_scoops_and_render.py 5 tests). Independently verified by orchestrator: all 3 sections render with working deep-links (chapter &t=90s cross-link, X permalink, corroborated tag, SCOOP); M1 no-M3-data path renders no M3 section elements (only CSS class defs in stylesheet, expected). Final suite: 115 passed.

## Phase-level passes (Step 3) — ALL PASS
- **3a DoD: PASS** — 115 tests green (uv run --with pytest pytest tests/); the unified stream clusters short-merge/long-cross-link; right-rail shows baseline-relative internal trending tagged corroborated-vs-scoop; scoops strip flags dormant-acceleration; rendered HTML shows all 3 previously-empty M3 sections with working deep-links (orchestrator-verified end-to-end on fixtures); NO embedding/vector dependency added (grep clean: no sentence-transformers/openai/faiss/numpy-cosine/anthropic; pyproject/uv.lock unchanged).
- **3b Slop scan: PASS — 5 findings fixed** — 5 stale docstring cross-references (`lib.trending.detect_scoops`/`tag_external_corroboration`/`build_trending_multiplier_map` → `lib.external_trending.*`, functions relocated in the trending.py split) corrected in rerank.py/render.py/web_search_keyless.py. No TODO/FIXME/print/bare-except/dead-code; all excepts typed and degrade-with-log.
- **3c CSO-lite: PASS** — single external egress (web_search_keyless.py) is keyless, HTTPS-only fixed host (html.duckduckgo.com), no API key/secret/env-read, no PII/secret logged (lib.log redacts), bounded by depth (quick:3/default:8/deep:15), stdlib-urllib only (no new dep), urlopen failure degrades-with-log not crash. No medium/low findings → no cso-findings file.

## Phase-6 handoff — orbit.py M3 wiring (validated, intentionally NOT in this phase)
`orbit.py` is NOT in any Phase 5 sub-phase's Files-touched, so M3 was NOT wired into the pipeline driver here (lib functions + render/rerank wiring + tests only). A prior interrupted run had left an UNCOMMITTED draft `orbit.py` carrying a real `run_stage5_overlap_trending_scoops(...)` seam + a `trending_multipliers=` thread into `run_stage6_rank_and_tier`. The orchestrator kept it out of the commit (correct — out of scope) but left it dirty in the tree; the Phase-4 orchestrator (verifying close-out) restored `orbit.py` to the committed HEAD so the tree is CLEAN. The discarded draft was VALID against the committed lib signatures (`cluster_overlaps(items, config)`, `compute_internal_trending(clusters, items_by_id, store_module)`, `tag_external_corroboration(items, search_fn=, depth=)`, `detect_scoops(items)`, `build_trending_multiplier_map(items)`, `derank_items(..., trending_multipliers=)`). **Phase 6 should re-add that Stage-5 seam** between classify (Stage 2) and rank (Stage 6): cluster → internal trending → external tag → scoops → multiplier map, then pass the map to Stage 6 and the clusters/trending/scoops to Stage 7's three render params. Empty `items` must return neutral (`[], [], [], {}`) so the M1/M2 quiet path stays byte-for-byte unchanged. This is M4-integration work, gated behind a runtime LLM for classify (absent in this build env).

## STATUS: COMPLETE — committed atomically at Step 4 (67a393e). Tree clean (orbit.py restored to HEAD post-commit).
